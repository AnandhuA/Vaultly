<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/vaultly_widget_background"
    android:orientation="vertical"
    android:padding="14dp">

    <TextView
        android:id="@+id/widgetTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Vaultly"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widgetSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="Save anything"
        android:textColor="#DDE2FF"
        android:textSize="12sp" />

    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:layout_weight="1"
        android:columnCount="2"
        android:rowCount="2">

        <TextView
            android:id="@+id/widgetClipboard"
            style="@style/VaultlyWidgetButton"
            android:text="Clipboard" />

        <TextView
            android:id="@+id/widgetNote"
            style="@style/VaultlyWidgetButton"
            android:text="Note" />

        <TextView
            android:id="@+id/widgetLink"
            style="@style/VaultlyWidgetButton"
            android:text="Link" />

        <TextView
            android:id="@+id/widgetSearch"
            style="@style/VaultlyWidgetButton"
            android:text="Search" />
    </GridLayout>
</LinearLayout>
